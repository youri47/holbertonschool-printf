#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <unistd.h>

/* Prototypes de toutes tes fonctions */
int _putchar(char c);
int print_number(int n);
int _printf(const char *format, ...);

#endif /* MAIN_H */
